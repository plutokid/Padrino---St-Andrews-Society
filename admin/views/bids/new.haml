.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:bids, :index)
      %li.active=link_to pat(:new), url(:bids, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:bid)
    .inner
      -form_for :bid, url(:bids, :create), :class => :form do |f|
        =partial "bids/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")